import { ChangecolorPipe } from './changecolor.pipe';

describe('ChangecolorPipe', () => {
  it('create an instance', () => {
    const pipe = new ChangecolorPipe();
    expect(pipe).toBeTruthy();
  });
});
